#
#    build:
#        runs-on: ubuntu-latest
#
#        steps:
#        - name: Checkout code
#            uses: actions/checkout@v2
#
#        - name: Set up JDK 17
#            uses: actions/setup-java@v2
#            with:
#            java-version: '17'
#            distribution: 'temurin'
#
#        - name: Build with Maven
#            run: mvn clean install
#
#        - name: Build Docker images
#            run: |
#            docker build -t discovery-service -f Dockerfile-discovery-service .
#            docker build -t config-service -f Dockerfile-config-service .
#            docker build -t gateway-service -f Dockerfile-gateway-service .
#            docker build -t product-service -f Dockerfile-product-service .
#
#        - name: Run Docker containers
#            run: |
#            docker run -d --name discovery-service --network app-network discovery-service
#            docker run -d --name config-service --network app-network config-service
#            docker run -d --name gateway-service --network app-network gateway-service
#            docker run -d --name product-service --network app-network product-service
#
#        - name: Run tests
#            run: mvn test
#
#        - name: Push Docker images to registry
#            env:
#            DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
#            DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
#            run: |
#            echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
#            docker tag discovery-service ${{ secrets.DOCKER_USERNAME }}/discovery-service
#            docker tag config-service ${{ secrets.DOCKER_USERNAME }}/config-service
#            docker tag gateway-service ${{ secrets.DOCKER_USERNAME }}/gateway-service
#            docker tag product-service ${{ secrets.DOCKER_USERNAME }}/product-service